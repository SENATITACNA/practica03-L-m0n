<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= user ? 'Editar' : 'Nuevo' %> Usuario</title>

  </head>
  <body>
    <h1><%= user ? 'Editar' : 'Nuevo' %> Usuario</h1>

    <form
      method="POST"
      action="<%= user ? '/actualizar/' + user.id : '/crear' %>">
      
      <input
        type="text"
        name="nombre"
        placeholder="Nombre"
        value="<%= user ? user.nombre : '' %>"
        required
      /><br/><br/>

      <input
        type="email"
        name="correo"
        placeholder="Correo"
        value="<%= user ? user.correo : '' %>"
        required
      /><br/><br/>

      <button type="submit">Guardar</button>

    </form>
  </body>
</html>
